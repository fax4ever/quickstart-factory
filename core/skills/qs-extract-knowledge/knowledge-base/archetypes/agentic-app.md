---
name: agentic-app
description: "Agent-based AI app with tool use, multi-framework runners, and orchestrated workflows on RHOAI"
summary: "Archetype for deploying AI agents on RHOAI that reason, invoke tools via MCP protocol, and orchestrate multi-step workflows -- seven approaches (A-G) cover conversational agent platforms, event-driven pipelines, framework-less MCP-native agents, CloudEvent-driven multi-microservice systems, operator-managed agents with full observability, regulated-industry multi-persona platforms with compliance controls, and MCP-server-centric observability agents where the application IS the tool server. Choose over rag-chatbot when AI must take actions beyond retrieval and over model-serving-app when an orchestration layer wraps the served model; pick A (ai-virtual-agent) for general-purpose interactive tool use with pluggable runners (LlamaStack/LangGraph/CrewAI) and configurable guardrails, B (ansible-log-analysis) for Grafana-alert-triggered LangGraph pipelines with nested sub-agents (main graph -> get_more_context_agent -> loki_agent) and Loki MCP querying, C (data-governance-co-pilot) for switchable MCPDirectProvider vs LlamaStackProvider orchestration via COPILOT_PROVIDER_MODE with Pydantic-validated ALLOWED_TOOLS fail-closed allowlist and Nemotron/OpenAI dual format auto-detection with think-tag streaming separation, D (it-self-service-agent) for enterprise IT self-service with 3 CloudEvent/Knative/Kafka FastAPI microservices and YAML-configurable LangGraph state machines with router-specialist topology and multi-channel integration (Slack/email/Zammad), E (lls-observability) for observability-first deployment via operator-managed LlamaStackDistribution CR with standalone MCP servers (weather/HR/OpenShift ops with read-only RBAC), Llama Guard 3-1B safety, comprehensive OTel sidecar injection for vLLM traces+metrics and LlamaStack traces with TempoStack+Grafana+UWM PodMonitors, optional MaaS integration (maas.enabled via LiteLLM), and zero custom backend code, F (multi-agent-loan-origination) for regulated-industry with 5 persona-specific LangGraph agents, Keycloak RBAC per-tool allowed_roles and per-role DataScope (PII masking/own-data-only), NeMo Guardrails fail-closed input/output shield nodes (/v1/guardrail/checks API with NemoGuard 8B), SHA-256 hash-chained audit trails with PostgreSQL advisory locks, HMDA data isolation enforced by lint-hmda-isolation CI, tier-boosted pgvector compliance KB (federal 1.5x > agency 1.2x > internal 1.0x), human-in-the-loop two-phase decisions (PROPOSE then CONFIRM), vision document extraction (pymupdf + LLM fallback), FastMCP risk server (Streamable HTTP), A2A/Kagenti with SPIRE mTLS, structured compliance checks (ECOA/ATR-QM/TRID) gating underwriting, and three-tier evaluation (MLflow GenAI + KFP + EvalHub), G (openshift-ai-observability-summarizer) for platform observability intelligence with 40+ FastMCP tools querying Prometheus/Tempo/Korrel8r/vLLM, multi-provider chatbot factory (OpenAI/Anthropic/Google/Meta) with MCPServerAdapter for in-process tool calling, OpenShift Console Plugin, Helm Operator (AIObservabilitySummarizer CR) lifecycle, alerting CronJob (Alertmanager + LlamaStack + Slack), report generation (HTML/PDF/Markdown), and curated metrics catalog with dynamic Prometheus validation. Critical patterns: A stacks vLLM/LlamaStack with function calling behind FastAPI managing agent lifecycle and SSE streaming with React/PatternFly and pgvector; B uses dual OpenAI-compatible endpoints with FAISS RAG, Gradio, DeepEval annotation, and Arize Phoenix + Grafana + Alloy observability; C deploys KServe+vLLM (Nemotron Nano 9B or Qwen3-14B AWQ) with auto-converted MCP-to-OpenAI tools, governance policy injection via REST API, and SvelteKit+Vega-Lite; D deploys shared-models/shared-clients libraries with LlamaStack+vLLM (Llama 3 70B) via ai-architecture-charts, PostgreSQL-backed LangGraph checkpoints with session locking, MCP servers for ServiceNow/Zammad, and Langfuse+MLflow+OTel+ClickHouse observability; E deploys custom OTel-instrumented vLLM image supporting GPU and Xeon CPU via conditional Helm values, 3 standalone MCP Helm charts, inline Milvus+sentence-transformers RAG, and Streamlit Playground+Jupyter notebooks; F deploys model-server-agnostic (any OpenAI-compatible via LLM_BASE_URL) with tiered model routing (llm/vision/embedding via config/models.yaml), MLflow autolog with Kubernetes auth, Prometheus custom metrics (LLM tokens/latency/routing/tool calls), and React (Radix UI+TanStack Router+Tailwind CSS); G deploys umbrella aiobs-stack chart via Helm Operator spanning infrastructure (MinIO/TempoStack/LokiStack/OTel Collector/Korrel8r) and application (MCP server/console plugin/alerting/RAG) across multiple namespaces with optional RAG stack via ai-architecture-charts subcharts. Common gotchas: E requires 5 OLM operators with strict phased deployment ordering (operators -> observability -> AI services -> MCP servers), D requires Knative Eventing + Kafka Broker (Strimzi-managed) infrastructure overhead with session locking (acquire_agent_session_lock/release_agent_session_lock) for concurrent handling plus mock services for development, F requires fail-closed guardrails for regulated domains with PostgreSQL advisory lock serialization for hash-chain consistency and structured compliance check gating (FAIL blocks underwriting approval), G requires cluster-admin for console plugin and cluster-wide observability RBAC (ClusterRoleBinding for Prometheus/Thanos/Tempo/Loki) plus SSE timeout Route annotation (haproxy.router.openshift.io/timeout: 300s via reencrypt TLS), and C requires MCP session reconnection with exponential backoff for pod restarts and dual tool-calling format handling (Nemotron custom TOOLCALL tags vs standard OpenAI) with auto-detection from model name; simple prompt-response or document-only QA should use model-serving-app or rag-chatbot instead."
metadata:
  type: archetype
tags:
  tech_stack: [fastapi, react, patternfly, postgresql, python, gradio, langchain, langgraph, sveltekit, vega-lite, mcp, fastmcp, openai-sdk, pydantic, knative, kafka, redis, langfuse, mlflow, deepeval, streamlit, jupyter, opentelemetry, grafana, tempo, loki, korrel8r, radix-ui, tanstack-router, tailwind-css, keycloak, minio, pymupdf, sentence-transformers, prometheus-client, kagenti, nemo-guardrails, llama-stack-client]
  ai_pattern: [agents, rag, guardrails, model-serving, embeddings, evaluation, tool-calling, governance, observability, distributed-tracing, pii-masking, audit-trail, compliance, vision-extraction, human-in-the-loop, a2a-protocol, metrics-analysis, report-generation, natural-language-querying]
  platform: [rhoai, openshift, vllm, tei, kserve, llama-stack, knative-eventing, llama-stack-operator, evalhub, kubeflow-pipelines, helm-operator, openshift-console-plugin]
  data_layer: [pgvector, faiss, clickhouse, milvus]
source_examples:
  - quickstart: "ai-virtual-agent"
    repo: "https://github.com/rh-ai-quickstart/ai-virtual-agent"
    notes: "Multi-runner agent platform (LlamaStack, LangGraph, CrewAI) with MCP tool integration, guardrails, and RAG knowledge bases"
    approach: "A"
  - quickstart: "ansible-log-analysis"
    repo: "https://github.com/rh-ai-quickstart/ansible-log-analysis"
    notes: "Event-driven LangGraph agent pipeline for automated Ansible log analysis with MCP-based Loki querying, error classification, expert routing, and step-by-step remediation"
    approach: "B"
  - quickstart: "data-governance-co-pilot"
    repo: "https://github.com/rh-ai-quickstart/data-governance-co-pilot"
    notes: "Framework-less MCP-native agent with provider abstraction (MCP-Direct self-managed loop vs Llama Stack delegated orchestration), pg-airman-mcp for PostgreSQL governance, hard-coded tool allowlist security, and dual model format support (Nemotron vs OpenAI)"
    approach: "C"
  - quickstart: "it-self-service-agent"
    repo: "https://github.com/rh-ai-quickstart/it-self-service-agent"
    notes: "CloudEvent-driven multi-microservice agent platform (agent-service, request-manager, integration-dispatcher) with YAML-configurable LangGraph state machines, router-specialist agent topology, Knative Eventing + Kafka, multi-channel integration (Slack, email, Zammad), MCP servers for ServiceNow/Zammad, NeMo Guardrails, Langfuse + MLflow + OpenTelemetry observability, and DeepEval evaluation pipeline"
    approach: "D"
  - quickstart: "lls-observability"
    repo: "https://github.com/rh-ai-quickstart/lls-observability"
    notes: "Operator-managed LlamaStackDistribution CR with MCP servers (weather, HR API, OpenShift ops), Llama Guard safety shields, comprehensive OTel + Tempo + Grafana observability with sidecar injection for both vLLM and LlamaStack, multi-device vLLM (GPU/Xeon), Llama Stack Playground + Jupyter notebooks (RAG, evals, LangGraph agent/tools)"
    approach: "E"
  - quickstart: "multi-agent-loan-origination"
    repo: "https://github.com/rh-ai-quickstart/multi-agent-loan-origination"
    notes: "Multi-persona LangGraph agent platform for regulated mortgage lending with 5 role-specific agents (public, borrower, loan officer, underwriter, CEO), Keycloak RBAC with per-tool allowed_roles and per-role data scopes, NeMo Guardrails input/output shields in LangGraph graph nodes, hash-chained audit trails with PostgreSQL advisory locks, HMDA demographic data isolation, compliance KB with tier-boosted pgvector search, vision-based document extraction (pymupdf + LLM vision fallback), MCP risk assessment server (FastMCP + Streamable HTTP), A2A protocol via Kagenti AgentRuntime CR, PII masking middleware, human-in-the-loop two-phase decision flow, MLflow observability with Kubernetes auth, three-tier evaluation (MLflow GenAI scorers + KFP pipeline + TrustyAI EvalHub), Prometheus custom metrics, and React (Radix UI + TanStack Router + Tailwind CSS) frontend"
    approach: "F"
  - quickstart: "openshift-ai-observability-summarizer"
    repo: "https://github.com/rh-ai-quickstart/openshift-ai-observability-summarizer"
    notes: "MCP-server-centric observability agent -- FastMCP server providing Prometheus, Tempo, Korrel8r, vLLM, and OpenShift analysis tools with multi-provider chatbot factory (OpenAI, Anthropic, Google, Meta, deterministic) acting as the agent layer, OpenShift Console Plugin for native UI integration, Helm Operator (AIObservabilitySummarizer CR) for lifecycle management, alerting CronJob with LlamaStack + Slack, report generation (HTML/PDF/Markdown), and full observability stack (TempoStack, LokiStack, OTel Collector, Korrel8r)"
    approach: "G"
---

# Agentic App

## Overview

An agentic app deploys one or more AI agents that can reason, use tools, and take actions to accomplish tasks on behalf of users. Unlike simple chat interfaces that only generate text, agentic apps integrate tool ecosystems (web search, databases, external APIs) and can orchestrate multi-step workflows. On RHOAI, these apps leverage model serving for LLM inference while the agent orchestration layer manages tool dispatch, conversation state, and safety guardrails.

## Typical Components

- **Model serving:** vLLM or LlamaStack for LLM inference (chat completions with tool/function calling support)
- **Backend:** FastAPI handling agent lifecycle, session management, and streaming responses
- **Frontend:** React/PatternFly providing agent configuration UI and real-time chat with SSE streaming
- **Data layer:** PostgreSQL for agent/session persistence, pgvector for knowledge base vector search
- **Supporting:** MCP servers for tool integration, guardrails for safety controls, ingestion pipelines for knowledge bases

## When to Use

- **Business problem:** Automating customer interactions or internal workflows that require the AI to take actions (search the web, query databases, call APIs) rather than just answer questions from static knowledge
- **RHOAI capabilities:** Demonstrates model serving with tool/function calling, agent orchestration across multiple frameworks, MCP protocol for extensible tool integration, and configurable guardrails
- **Scale/complexity:** Medium to high complexity; suitable when a simple prompt-response flow is insufficient and the application needs multi-step reasoning with external tool invocations

## Example Quickstarts

| Quickstart | What It Demonstrates |
|------------|---------------------|
| ai-virtual-agent | Multi-framework agent platform with pluggable runners (LlamaStack, LangGraph, CrewAI), MCP tool servers, configurable guardrails, and RAG-backed knowledge bases |
| ansible-log-analysis | Event-driven LangGraph agent pipeline that detects Ansible errors from Grafana alerts, classifies and routes them to domain experts, retrieves additional log context via MCP-based Loki queries, and generates step-by-step remediation |
| data-governance-co-pilot | Framework-less MCP-native agent with dual provider modes (MCP-Direct and Llama Stack), pg-airman-mcp for PostgreSQL governance tools, hard-coded tool allowlist with Pydantic validation, governance policy injection, and SvelteKit + Vega-Lite frontend |
| it-self-service-agent | CloudEvent-driven multi-microservice platform with YAML-configurable LangGraph state machines, router-specialist agent topology, Knative Eventing + Kafka for inter-service communication, multi-channel integration (Slack, email, Zammad), MCP servers for ServiceNow ticketing, NeMo Guardrails, and Langfuse + MLflow + OpenTelemetry observability |
| lls-observability | Operator-managed Llama Stack agent with comprehensive LLM observability -- LlamaStackDistribution CR via llama-stack-operator, standalone MCP servers (weather, HR API, OpenShift ops), Llama Guard safety shields, OTel sidecar injection for vLLM and LlamaStack traces/metrics, TempoStack distributed tracing, Grafana dashboards, User Workload Monitoring PodMonitors, and Jupyter notebooks for RAG, evals, and LangGraph agent demos |
| multi-agent-loan-origination | Multi-persona regulated-industry agent platform with 5 role-specific LangGraph agents (public, borrower, loan officer, underwriter, CEO), Keycloak RBAC with per-tool role restrictions and per-role data scoping (PII masking for CEO, own-data-only for borrower), NeMo Guardrails input/output shields integrated in LangGraph graph nodes, hash-chained audit trails, HMDA demographic data isolation, compliance KB with tier-boosted pgvector search, vision-based document extraction, MCP risk assessment server, A2A/Kagenti integration, human-in-the-loop decisions, MLflow + Prometheus observability, and three-tier evaluation (MLflow GenAI + KFP + EvalHub) |
| openshift-ai-observability-summarizer | MCP-server-centric platform observability agent -- FastMCP server with 40+ tools for Prometheus PromQL, OpenShift cluster analysis, vLLM model metrics, Tempo tracing, and Korrel8r signal correlation; multi-provider chatbot factory (OpenAI, Anthropic, Google, Meta) as the agent layer; OpenShift Console Plugin for native console integration; Helm Operator (AIObservabilitySummarizer CR) for lifecycle management; alerting CronJob with LlamaStack + Slack; report generation (HTML/PDF/Markdown); full observability stack (TempoStack, LokiStack, OTel Collector, MinIO, Korrel8r) |

## Decision Criteria

### vs rag-chatbot

Pick **agentic-app** when the AI needs to take actions beyond retrieval -- calling external APIs, executing multi-step workflows, or using tools dynamically selected at runtime. Pick **rag-chatbot** when the primary interaction is question-answering grounded in uploaded documents, without tool orchestration or multi-step agent reasoning.

### vs model-serving-app

Pick **agentic-app** when the application includes an orchestration layer that manages agent state, tool dispatch, and multi-turn reasoning on top of the served model. Pick **model-serving-app** when the focus is on deploying and exposing a model endpoint (KServe/vLLM) without an agent framework wrapping it.

---

## Approach B: Event-Driven Agent Pipeline (from ansible-log-analysis)

### When to Use

When the agentic app is triggered by external events (alerts, webhooks, log streams) rather than user conversations, and the agent follows a domain-specific multi-step pipeline (detect, classify, route, gather context, remediate) rather than open-ended conversational tool use.

### Differences from Approach A

- **Triggering model:** Approach A is user-initiated (conversational chat); Approach B is event-driven (Grafana alerts trigger the pipeline via webhook to `backend-inference-webhook` in `deploy/helm/ansible-log-monitor/values.yaml`)
- **Agent framework:** Approach A supports pluggable multi-framework runners (LlamaStack, LangGraph, CrewAI); Approach B uses LangGraph exclusively with a fixed multi-step graph (`src/alm/agents/graph.py`)
- **Agent topology:** Approach A has a single agent with tool access; Approach B composes a main graph with nested sub-agents -- a `get_more_context_agent` that decides whether to invoke a `loki_agent` sub-graph for additional log retrieval (`src/alm/agents/get_more_context_agent/graph.py`, `src/alm/agents/loki_agent/graph.py`)
- **Pipeline structure:** Approach B follows a fixed domain-specific flow: cluster logs -> summarize -> classify error type -> route to expert domain -> decide if more context needed -> query Loki via MCP -> summarize Loki context -> generate step-by-step solution (`src/alm/agents/graph.py`)
- **MCP usage:** Both use MCP, but differently -- Approach A uses MCP servers as general-purpose tool providers; Approach B uses a custom `MCPClient` class (`src/alm/mcp/mcp_client.py`) to query a Loki MCP server for domain-specific log retrieval with JSON-RPC 2.0 protocol
- **Tool definition:** Approach B defines LangChain `@tool`-decorated functions for specific Loki query patterns (file logs, text search, play recaps, log lines above) in `src/alm/tools/loki_tools.py`
- **Model serving:** Approach A deploys vLLM/LlamaStack for self-hosted inference; Approach B connects to any OpenAI-compatible endpoint via `OPENAI_API_ENDPOINT` env var (`src/alm/llm.py`), with a separate endpoint for tool-calling-capable models (`OPENAI_API_ENDPOINT_WITH_TOOL_CALLING`)
- **RAG integration:** Approach A uses pgvector for knowledge base vector search; Approach B uses a separate FAISS-based RAG service (`services/rag/`) with HuggingFace TEI for embeddings, providing "cheat sheet" context from Ansible error documentation PDFs
- **Frontend:** Approach A uses React/PatternFly; Approach B uses Gradio for both the main UI (`services/ui/`) and an annotation/evaluation interface (`services/annotation_interface/`) with DeepEval
- **Observability:** Approach A has no built-in observability; Approach B includes Arize Phoenix for LLM tracing (`src/alm/utils/phoenix.py`), Grafana for dashboards, and Alloy for log ingestion (`services/alloy/`)
- **Log management stack:** Approach B includes a full log management pipeline -- Grafana Alloy collects logs from AAP clusters, Loki stores and indexes them, Grafana provides alerting on error patterns, and the agent queries Loki via MCP for additional context

### Typical Components

- **Model serving:** Any OpenAI-compatible API endpoint (RHOAI model serving, external LLM), with separate endpoint option for tool-calling models
- **Backend:** FastAPI (`src/alm/`) with LangGraph agent orchestration, MCP client for Loki tool integration, LangChain tools
- **Frontend:** Gradio UI for log analysis dashboard (`services/ui/`), Gradio annotation interface with DeepEval for evaluation (`services/annotation_interface/`)
- **Data layer:** PostgreSQL for alert/metadata persistence, FAISS for RAG vector storage (Ansible error documentation), Loki for time-series log storage
- **Supporting:** HuggingFace TEI for text embeddings, MinIO for object storage, Arize Phoenix for LLM tracing, Grafana + Alloy for log ingestion and alerting, MCP server for Loki queries, sentence-transformers + scikit-learn for log clustering (`services/clustering/`)

---

## Approach C: Framework-less MCP-Native Agent with Provider Abstraction (from data-governance-co-pilot)

### When to Use

When the agentic app needs direct control over the agentic loop without framework dependencies, connects to a single specialized MCP server for domain-specific tool use (e.g., database governance), and benefits from the ability to switch between a self-managed agentic loop (MCP-Direct) and a delegated orchestration mode (Llama Stack) via a provider abstraction layer.

### Differences from Approach A

- **Agent framework:** Approach A uses pluggable framework runners (LlamaStack, LangGraph, CrewAI); Approach C uses no agent framework in MCP-Direct mode -- the agentic loop is hand-coded in `packages/copilot/src/copilot/providers/mcp_direct.py` using the MCP SDK (`mcp.ClientSession`) and OpenAI SDK (`AsyncOpenAI`) directly, with an alternative Llama Stack provider (`packages/copilot/src/copilot/providers/llama_stack.py`) that delegates orchestration to Llama Stack's Agents API
- **Provider abstraction:** Approach C introduces a `LLMProvider` abstract base class (`packages/copilot/src/copilot/providers/base.py`) with `MCPDirectProvider` and `LlamaStackProvider` implementations, switchable via the `COPILOT_PROVIDER_MODE` environment variable (`packages/copilot/src/copilot/providers/factory.py`); Approach A uses pluggable runners within a single framework
- **MCP integration:** Approach A uses MCP servers as general-purpose tool providers; Approach C connects to a single specialized MCP server (EDB's `pg-airman-mcp` for PostgreSQL) via the official MCP SDK's `streamablehttp_client` transport (`packages/copilot/src/copilot/providers/mcp_direct.py` line 31), with MCP tool definitions automatically converted to OpenAI function calling format (`_convert_mcp_tools_to_openai` method)
- **Tool security:** Approach C implements a hard-coded tool allowlist with Pydantic schema validation in `packages/copilot/src/copilot/providers/tool_validation.py` -- every tool call is validated against `ALLOWED_TOOLS` set and type-checked via Pydantic models (`ExecuteSqlArgs`, `ListObjectsArgs`, etc.) before execution, with fail-closed behavior (unknown tools are rejected even if MCP server advertises them); Approach A delegates tool validation to the framework runners
- **Model format detection:** Approach C supports two tool calling formats -- Nemotron's custom `<TOOLCALL>` tags parsed by `_parse_nemotron_tool_calls()` and standard OpenAI function calling -- with auto-detection from model name (`_detect_tool_call_format` in `mcp_direct.py`); configurable via `LLM_TOOL_CALL_FORMAT` env var (`auto`/`nemotron`/`openai`)
- **Governance policy:** Approach C makes data governance policy a first-class concept -- policies can be uploaded/deleted via REST API (`/policy/upload`, `/policy/status`, `/policy/delete` in `service.py`), injected into the system prompt dynamically for MCP-Direct mode (no conversation restart needed), or trigger agent recreation for Llama Stack mode (requires conversation restart per `requires_conversation_restart_on_policy_update()`)
- **MCP session resilience:** Approach C includes MCP session reconnection logic (`_reconnect_mcp` method) and retry with exponential backoff (`_retry_mcp_operation` method) for handling pod restarts and transient failures
- **Thinking tag handling:** Approach C handles Nemotron's `<think>` and `</think>` tags during streaming, separating LLM reasoning from user-facing content, with support for both `enable_reasoning=True` (show thinking) and `enable_reasoning=False` (silently discard via Nemotron's `/no_think` instruction)
- **Frontend:** Approach A uses React/PatternFly; Approach C uses SvelteKit with Vega-Lite for inline data visualizations (bar charts, pie charts, line charts) generated by the LLM in `vega-lite` code blocks (`packages/copilot/src/copilot/providers/mcp_direct.py` system prompt lines 433-466)
- **Model options:** Approach C supports two models -- NVIDIA Nemotron Nano 9B v2 (9B params, custom TOOLCALL format, MCP-Direct only) and Qwen3-14B AWQ (14B params, OpenAI function calling, both modes) -- deployed via KServe + vLLM (`helm/nemotron-model/`, `helm/qwen3-model/`)

### Typical Components

- **Model serving:** KServe + vLLM deploying either NVIDIA Nemotron Nano 9B v2 or Qwen3-14B AWQ; optional Llama Stack Distribution for delegated agent orchestration (`helm/copilot-llama-stack/`)
- **Backend:** FastAPI (`packages/copilot/`) with provider abstraction layer, direct MCP SDK + OpenAI SDK agentic loop (MCP-Direct) or Llama Stack Agents API delegation, SSE streaming, tool validation allowlist
- **Frontend:** SvelteKit (`apps/ui/`) with Vega-Lite for inline data visualizations, Markdown rendering, SQL syntax highlighting
- **Data layer:** PostgreSQL + pgvector (`helm/pgvector/`) for the governed e-commerce loyalty dataset (not used for RAG), EDB pg-airman-mcp (`helm/pg-airman-mcp/`) for MCP-based database tools
- **Supporting:** MinIO for object storage (`helm/minio/`), pgAdmin for database administration (`helm/pgadmin/`), Jupyter notebooks for model download and data verification (`notebooks/`)

---

## Approach D: CloudEvent-Driven Multi-Microservice Agent Platform (from it-self-service-agent)

### When to Use

When the agentic app requires a production-grade multi-microservice architecture with event-driven inter-service communication, multi-channel integration (Slack, email, ticketing systems), YAML-configurable agent workflows for adding new use cases without code changes, and a router-specialist agent topology where a routing agent classifies user intent and hands off to domain-specific specialist agents.

### Differences from Approach A

- **Service architecture:** Approach A is a monolithic FastAPI service; Approach D decomposes into three core microservices -- `agent-service` (LangGraph agent orchestration, knowledge bases, MCP tool integration), `request-manager` (request validation, session lifecycle, communication strategy), and `integration-dispatcher` (multi-channel delivery to Slack, email, Zammad, webhooks) -- each as separate FastAPI deployments with shared-models and shared-clients libraries (`shared-models/`, `shared-clients/`)
- **Inter-service communication:** Approach A uses direct HTTP; Approach D uses CloudEvents over Knative Eventing with a Kafka Broker (`helm/templates/knative-broker.yaml`, `helm/templates/knative-triggers.yaml`) -- events like `com.self-service-agent.request.created` and `com.self-service-agent.agent.response-ready` route between services via Knative Triggers with per-partition FIFO ordering (`kafka.eventing.knative.dev/delivery.order: ordered`) and exponential backoff retry; a mock eventing service (`mock-eventing-service/`) enables development without Knative infrastructure
- **Agent topology:** Approach A has a single agent with tool access; Approach D uses a router-specialist topology -- a `routing-agent` (`agent-service/config/agents/routing-agent.yaml`) classifies user intent (laptop refresh, email change, etc.) and hands the session off to a specialist agent (e.g., `laptop-refresh-agent` with its own MCP servers and knowledge bases); specialist agents can return users to the routing agent upon task completion via `task_complete_return_to_router`
- **YAML-driven state machines:** Approach A uses framework-provided agent configuration; Approach D defines LangGraph state machines entirely in YAML (`agent-service/config/lg-prompts/*.yaml`) with configurable states (e.g., `waiting`, `llm_processor`), transitions, prompts, temperature settings, and business fields -- the `StateMachine` class (`agent-service/src/agent_service/langgraph/lg_flow_state_machine.py`) dynamically creates `AgentState` TypedDict classes from YAML `state_schema` and builds `StateGraph` instances from YAML `states` definitions, enabling new agent workflows without code changes
- **Agent configuration:** Approach D defines each agent in a declarative YAML file (`agent-service/config/agents/<name>.yaml`) specifying `name`, `system_message`, `lg_state_machine_config` (path to LangGraph prompt YAML), `mcp_servers` (list of MCP server URIs), `knowledge_bases` (list of RAG knowledge base names), and `sampling_params` (temperature, top_p)
- **Multi-channel integration:** Approach A provides a single React/PatternFly frontend; Approach D supports multiple inbound channels -- Slack (real-time conversations via Bolt SDK), email (asynchronous notifications), Zammad (ticketing system with CloudEvent-driven ticket lifecycle), CLI, and webhooks -- managed by the `integration-dispatcher` service with per-channel handlers (`integration-dispatcher/src/integration_dispatcher/integrations/slack.py`, `email.py`, `zammad.py`, `webhook.py`) and a template engine for formatting responses per channel
- **MCP usage:** Approach A uses MCP servers as general-purpose tool providers within a single agent; Approach D deploys dedicated MCP servers for specific backend integrations -- `mcp-servers/snow/` for ServiceNow ticket operations (laptop refresh requests) and `mcp-servers/zammad/` for Zammad ticketing -- using FastMCP with shared MCP utilities (`mcp-servers/mcp-common/`); MCP servers are deployed as separate pods via ai-architecture-charts `mcp-servers` subchart (`helm/Chart.yaml`)
- **Model serving:** Both use LlamaStack; Approach D deploys via ai-architecture-charts subcharts (`llm-service` 0.5.6, `llama-stack` 0.8.5, `pgvector` 0.1.0 in `helm/Chart.yaml`) and targets Llama 3 70B for agent reasoning, with LlamaStack providing both native and OpenAI-compatible APIs (`agent-service/src/agent_service/langgraph/responses_agent.py`)
- **Knowledge bases:** Approach A uses pgvector with document upload UI; Approach D uses LlamaStack vector stores with knowledge base YAML configuration files (`agent-service/config/knowledge_bases/`) registered at startup via `KnowledgeBaseManager` (`agent-service/src/agent_service/knowledge/kb_manager.py`) -- e.g., laptop refresh policy documents and laptop options per region
- **Guardrails:** Approach A has configurable guardrails within the agent framework; Approach D optionally integrates NVIDIA NeMo Guardrails with NemoGuard JailbreakDetect NIM (`helm/nemo-guardrails/`) via TrustyAI, controlled by `USE_NEMO_GUARDRAILS` env var (`agent-service/src/agent_service/langgraph/responses_agent.py`) -- the guardrails service runs as a separate deployment with its own GPU allocation
- **Observability:** Approach A has no built-in observability; Approach D includes a comprehensive stack -- Langfuse for session-level LLM tracing (`LANGFUSE_ENABLED` env var, ClickHouse analytics backend in `helm/templates/clickhouse-deployment.yaml`, `helm/templates/langfuse-deployment.yaml`), MLflow for per-turn LLM call tracking (`agent-service/src/agent_service/langgraph/mlflow_tracking.py`), OpenTelemetry for distributed tracing across all three microservices (`tracing-config/`), and Redis for caching (`helm/templates/redis-deployment.yaml`)
- **Database schema:** Approach A uses a simple PostgreSQL schema; Approach D uses shared SQLAlchemy models with Alembic migrations (`shared-models/alembic/`) covering request sessions, delivery logs, user integration configs, and channel behavior policies -- the database is shared across all three microservices via the `shared-models` library
- **Mock services for development:** Approach D includes mock services for developing without external dependencies -- `mock-service-now/` (mock ServiceNow REST API), `mock-employee-data/` (mock employee and laptop data), `mock-eventing-service/` (lightweight CloudEvent relay replacing Knative/Kafka)
- **Evaluation pipeline:** Approach A has no built-in evaluation; Approach D includes a DeepEval-based evaluation framework (`evaluations/`) that generates synthetic conversations (`evaluations/flows/ticket_laptop_refresh/flow.py`), exports real conversations from the running system, and evaluates against business metrics (policy compliance, information gathering completeness)
- **Session management:** Approach D implements session locking with `acquire_agent_session_lock`/`release_agent_session_lock` for concurrent request handling (`agent-service/src/agent_service/main.py`), PostgreSQL-backed LangGraph checkpoint storage (`agent-service/src/agent_service/langgraph/postgres_checkpoint.py`), and session resumption across agent handoffs

### Typical Components

- **Model serving:** vLLM via ai-architecture-charts `llm-service` subchart for LLM inference (Llama 3 70B); LlamaStack via ai-architecture-charts `llama-stack` subchart for agent orchestration, knowledge base RAG, and OpenAI-compatible API
- **Backend:** Three FastAPI microservices -- `agent-service` (LangGraph state machines, agent routing, knowledge base management, MCP tool integration), `request-manager` (request normalization, session lifecycle, communication strategy), `integration-dispatcher` (multi-channel delivery with Slack, email, Zammad, webhook handlers and template engine)
- **Frontend:** CLI for direct agent interaction; Slack, email, and Zammad as conversational interfaces via the integration-dispatcher
- **Data layer:** PostgreSQL + pgvector (ai-architecture-charts `pgvector` subchart) for shared database (sessions, delivery logs, user configs) and LangGraph checkpoint storage; LlamaStack vector stores for knowledge base RAG; ClickHouse for Langfuse analytics
- **Supporting:** Knative Eventing with Kafka Broker (Strimzi-managed via `helm/templates/kafka-cluster.yaml`) for inter-service CloudEvent routing; MCP servers for ServiceNow (`mcp-servers/snow/`) and Zammad (`mcp-servers/zammad/`) via ai-architecture-charts `mcp-servers` subchart; NVIDIA NeMo Guardrails with NemoGuard JailbreakDetect NIM (`helm/nemo-guardrails/`); Langfuse for session-level LLM observability; MLflow for per-turn tracking; OpenTelemetry for distributed tracing; Redis for caching; MinIO for object storage; mock services for development without external dependencies

---

## Approach E: Operator-Managed Llama Stack Agent with Comprehensive Observability (from lls-observability)

### When to Use

When the agentic app needs operator-managed lifecycle for the Llama Stack instance (no custom backend code), standalone MCP servers deployed as separate Helm charts, Llama Guard safety shields, and comprehensive LLM observability instrumentation (distributed tracing, metrics, dashboards) as a first-class capability for audit, compliance, and performance monitoring of the entire AI stack.

### Differences from Approach A

- **Service architecture:** Approach A is a monolithic FastAPI backend managing agent lifecycle, session state, and SSE streaming; Approach E has no custom backend code -- the entire agent orchestration is delegated to a `LlamaStackDistribution` CR (`llamastack.io/v1alpha1` in `helm/03-ai-services/llama-stack-instance/templates/llamastackdistribution.yaml`) managed by the `llama-stack-operator` (`helm/01-operators/llama-stack-operator/`)
- **Operator lifecycle:** Approach E uses the Llama Stack Kubernetes operator to manage the Llama Stack instance lifecycle (deployment, scaling, upgrades); Approach A deploys Llama Stack as a standalone server within a custom application
- **MCP server deployment:** Approach A integrates MCP servers within the application; Approach E deploys each MCP server as a separate Helm chart (`helm/04-mcp-servers/mcp-weather/`, `helm/04-mcp-servers/hr-api/`, `helm/04-mcp-servers/openshift-mcp/`) with independent lifecycle, NetworkPolicies, and RBAC -- the OpenShift MCP server (`quay.io/hveeradh/ocp-mcp-server`) has read-only ClusterRole bindings for pods, services, deployments, routes, etc. (`helm/04-mcp-servers/openshift-mcp/values.yaml`)
- **MCP integration:** Approach E connects MCP servers to Llama Stack via the `remote::model-context-protocol` tool runtime provider, with MCP server URIs configured in `helm/03-ai-services/llama-stack-instance/values.yaml` under `mcpServers` and rendered as `mcp::<name>` toolgroups with `/sse` endpoint suffix in the ConfigMap (`helm/03-ai-services/llama-stack-instance/templates/configmap.yaml`)
- **Safety:** Approach A has configurable guardrails within the agent framework; Approach E deploys a dedicated Llama Guard 3-1B model via KServe InferenceService (`helm/03-ai-services/llama-guard/`) with its own vLLM ServingRuntime and GPU allocation, connected to Llama Stack as the `inline::llama-guard` safety provider with a registered shield (`shield_id: meta-llama/Llama-Guard-3-1B` in the ConfigMap)
- **Model serving:** Approach A deploys vLLM/LlamaStack within the application; Approach E deploys Llama 3.2 3B Instruct via a standalone KServe InferenceService with a custom OTel-instrumented vLLM image (`quay.io/rcarrata/vllm-otlp-tracing`) that sends traces directly to the OTel collector via `--otlp-traces-endpoint` ServingRuntime arg (`helm/03-ai-services/llama3.2-3b/values.yaml`), plus support for both GPU (`nvidia.com/gpu`) and Intel Xeon CPU (`VLLM_CPU_KVCACHE_SPACE`) device types with separate image, resource, node selector, and affinity configurations per device
- **Observability stack:** Approach A has no built-in observability; Approach E includes a comprehensive observability stack deployed across 5 OLM-managed operators (`helm/01-operators/`) and 5 observability components (`helm/02-observability/`):
  - **OpenTelemetry Collector** -- central collector in `observability-hub` namespace (`helm/02-observability/otel-collector/otel-collector.yaml`) receives OTLP traces/metrics from sidecars and scrapes Prometheus metrics from vLLM endpoints; sidecar collectors for both LlamaStack (`otel-collector-llamastack-sidecar.yaml`, traces pipeline) and vLLM (`otel-collector-vllm-sidecar.yaml`, traces + metrics pipelines with `localhost:8000` Prometheus scraping) injected via `sidecar.opentelemetry.io/inject` pod annotation
  - **TempoStack** -- distributed tracing backend (`helm/02-observability/tempo/`) with MinIO S3-compatible storage, multi-tenant mode (`tenants.mode: openshift`, tenant `dev`), and Gateway for authenticated trace querying
  - **Grafana** -- operator-managed instance (`helm/02-observability/grafana/`) with Prometheus (via Thanos Querier) and Tempo datasources, pre-built vLLM dashboard loaded from `opendatahub-io/llama-stack-demos` GitHub repo via `GrafanaDashboard` CR URL reference
  - **User Workload Monitoring** -- PodMonitors (`helm/02-observability/uwm/`) scraping vLLM metrics from pods matching `app: safety | llama32-3b | granite-8b | llama31-70b` labels at 30s intervals, plus KServe predictor pods (`app: isvc.llama3-2-3b-predictor`) at 15s intervals
  - **Distributed Tracing UI Plugin** -- OpenShift Console plugin (`helm/02-observability/distributed-tracing-ui-plugin/`) for in-console trace visualization
- **Namespace topology:** Approach A deploys in a single namespace; Approach E uses at least 2 namespaces -- `llama-serve` for AI services and MCP servers, `observability-hub` for the observability stack (Tempo, Grafana, central OTel collector), plus `openshift-monitoring` and `openshift-user-workload-monitoring` for cluster monitoring ConfigMaps
- **LlamaStack configuration:** Approach E defines the full Llama Stack configuration in a Helm-templated ConfigMap (`helm/03-ai-services/llama-stack-instance/templates/configmap.yaml`) including: `remote::vllm` inference providers (separate for chat and safety models), `inline::sentence-transformers` (all-MiniLM-L6-v2), `inline::milvus` vector_io with SQLite-backed kvstore, `inline::llama-guard` safety, `inline::meta-reference` agents with SQLite persistence, `inline::meta-reference` telemetry with configurable sinks (console, sqlite, OTEL via `OTEL_TRACE_ENDPOINT`/`OTEL_METRIC_ENDPOINT` env vars), `remote::model-context-protocol` tool runtime, `remote::brave-search`/`remote::tavily-search`/`remote::wolfram-alpha` tool runtimes, and `inline::rag-runtime` for RAG
- **MaaS support:** Approach E includes optional Model-as-a-Service integration (`maas.enabled: true` in values.yaml) to add a remote vLLM provider pointing to a LiteLLM endpoint for larger models like Llama-4-Scout-17B without local GPU resources
- **Frontend:** Approach A uses React/PatternFly; Approach E uses the Llama Stack Playground (`helm/03-ai-services/llama-stack-playground/`, Streamlit on port 8501) with chat, agents, and tools features enabled, connecting to the LlamaStackDistribution service (`http://llama-stack-instance-service:8321`)
- **Demo content:** Approach A provides an interactive web UI; Approach E includes 4 Jupyter notebooks (`docs/notebooks/`) demonstrating different interaction patterns: simple RAG (`1-simpleRAG.ipynb`), evaluations (`2-evals.ipynb`), LangGraph agent (`3-langgraph-agent.ipynb`), and LangGraph tools (`4-langgraph-tools.ipynb`)
- **Phased deployment:** Approach E organizes Helm charts into 4 dependency phases -- Phase 1: Operators (cluster-observability-operator, grafana-operator, otel-operator, tempo-operator, llama-stack-operator), Phase 2: Observability (tempo, otel-collector, grafana, uwm, distributed-tracing-ui-plugin), Phase 3: AI Services (llama3.2-3b, llama-guard, llama-stack-instance, llama-stack-playground), Phase 4: MCP Servers (mcp-weather, hr-api, openshift-mcp) -- with `Makefile` targets `install-operators`, `deploy-observability`, `deploy-ai`, and `install-all` orchestrating the sequence via shell scripts

### Typical Components

- **Model serving:** KServe InferenceService with custom OTel-instrumented vLLM image (`quay.io/rcarrata/vllm-otlp-tracing`) for Llama 3.2 3B Instruct with built-in tracing (`--otlp-traces-endpoint`, `--collect-detailed-traces all`); separate KServe InferenceService for Llama Guard 3-1B safety model; both support GPU and Intel Xeon CPU device types via conditional Helm values
- **Backend:** LlamaStackDistribution CR managed by llama-stack-operator -- no custom backend code; Llama Stack provides agents, inference, safety, vector_io, tool_runtime, eval, scoring, datasetio, and telemetry APIs
- **Frontend:** Llama Stack Playground (Streamlit, `quay.io/rh-aiservices-bu/llama-stack-playground:0.2.11`) with chat, agent, and tool UI; Jupyter notebooks for RAG, evaluation, and LangGraph agent demos
- **Data layer:** Inline Milvus (SQLite-backed) within Llama Stack for vector storage; inline sentence-transformers (all-MiniLM-L6-v2) for embeddings; SQLite for agent persistence, responses, eval, and metadata stores
- **Supporting:** 5 OLM-managed operators (Cluster Observability, Grafana, OpenTelemetry, Tempo, Llama Stack); central OTel Collector in `observability-hub` with Prometheus receiver scraping vLLM metrics and OTLP receiver for traces; OTel sidecar collectors for LlamaStack (traces) and vLLM (traces + metrics); TempoStack with MinIO for distributed trace storage; Grafana with Prometheus and Tempo datasources and pre-built vLLM dashboard; User Workload Monitoring PodMonitors for vLLM pods; Distributed Tracing UI Plugin for OpenShift Console; 3 standalone MCP servers (weather, HR enterprise API, OpenShift/Kubernetes ops) deployed as separate Helm charts with NetworkPolicies and RBAC

---

## Approach F: Multi-Persona Agent Platform with Compliance Controls (from multi-agent-loan-origination)

### When to Use

When the agentic app serves a regulated industry (financial services, healthcare, legal) and requires multiple persona-specific agents each with distinct tool sets and data access scopes, Keycloak-based RBAC that controls which tools each role can invoke, compliance knowledge retrieval with regulatory source tiering, tamper-evident audit trails, PII masking based on user role, human-in-the-loop decision gates, and vision-based document extraction -- all deployed as a full-stack monorepo with React frontend and FastAPI backend.

### Differences from Approach A

- **Agent topology:** Approach A has a single agent with tool access and pluggable framework runners; Approach F deploys 5 distinct LangGraph agents -- `public-assistant` (product info, affordability), `borrower-assistant` (application intake, document tracking, disclosure management), `loan-officer-assistant` (pipeline review, document quality, underwriting submission), `underwriter-assistant` (risk assessment, compliance checks, conditions, decisions), and `ceo-assistant` (analytics, masked-PII portfolio view) -- each defined by a YAML config in `config/agents/` with its own system prompt, tool list, and `model_routing.strategy`
- **Agent selection:** Approach A selects agents via user choice in the UI; Approach F routes to the correct agent based on the authenticated user's Keycloak role -- each chat endpoint (`routes/borrower_chat.py`, `routes/underwriter_chat.py`, etc.) maps to one agent, and the JWT token's role claim determines which endpoints the user can access
- **RBAC-scoped tool access:** Approach A delegates tool access to the framework; Approach F defines per-tool `allowed_roles` lists in each agent's YAML config (e.g., `kb_search` is restricted to `[loan_officer, underwriter, admin]`, `uw_render_decision` to `[underwriter, admin]`); the `AgentState` carries `tool_allowed_roles` and `user_role` fields through the LangGraph graph, and tools check authorization before execution
- **Data scope filtering:** Approach A has no role-based data scoping; Approach F builds a `DataScope` object per role via `build_data_scope()` (`core/auth.py`) -- borrowers see `own_data_only=True` (filtered by `user_id`), loan officers see `assigned_to=user_id`, CEOs see `pii_mask=True, document_metadata_only=True`, underwriters and admins see `full_pipeline=True`; SQL queries are filtered by `apply_data_scope()` before execution
- **PII masking:** Approach F includes a Starlette middleware (`middleware/pii.py`) that automatically masks SSN (to `***-**-1234`), DOB (to `YYYY-**-**`), and account numbers (to `****5678`) in JSON response bodies when `request.state.pii_mask` is True (set from the user's `DataScope`); Approach A has no PII masking
- **Guardrails integration:** Approach A has configurable guardrails within the framework; Approach F integrates NeMo Guardrails directly into the LangGraph state graph as `input_shield` and `output_shield` nodes (`agents/base.py`) -- input shield checks user messages before agent processing and short-circuits to END with a refusal if unsafe, output shield checks agent responses and replaces with a refusal if unsafe; shields use NeMo's `/v1/guardrail/checks` API for regex, PII detection, and content safety NIM without triggering full LLM inference (< 5s vs ~45s); both shields fail-closed on error for the regulated lending domain
- **NeMo Guardrails deployment:** Approach F deploys NeMo Guardrails as a separate service with its own vLLM-served NemoGuard 8B content safety detector model (`deploy/helm/mortgage-ai/templates/nemo-guardrails-model.yaml`), proxy (`nemo-guardrails-proxy.yaml`), and config (`nemo-guardrails-config.yaml`), feature-gated by `nemoGuardrails.enabled` in Helm values; Approach D integrates NeMo Guardrails as a separate deployment via TrustyAI
- **Audit trail:** Approach F writes append-only audit events with SHA-256 hash chaining (`services/audit.py`) -- each event's hash includes the previous event's hash for tamper evidence, serialized via PostgreSQL advisory lock (`AUDIT_LOCK_KEY = 900_001`) for consistent hash chain computation across concurrent writers; session_id enables LangFuse trace correlation; Approach A has no audit trail
- **HMDA demographic isolation:** Approach F isolates HMDA (Home Mortgage Disclosure Act) demographic data in a dedicated compliance schema with `services/compliance/hmda.py` as the sole permitted accessor -- enforced by a `lint-hmda-isolation` CI check; document extraction routes demographics through this module with per-field precedence rules (self_reported > document_extraction); no other approach has regulated data isolation
- **Compliance knowledge base:** Approach F includes a pgvector-backed compliance KB (`services/compliance/knowledge_base/`) with tier-boosted vector search -- federal regulations get 1.5x boost, agency guidelines 1.2x, internal policies 1.0x (`search.py`); the KB is seeded with simulated HMDA, ECOA, TRID, ATR/QM, and FCRA content; Approach A uses pgvector for general knowledge base RAG
- **Structured compliance checks:** Approach F provides programmatic compliance checks (`services/compliance/checks.py`) for ECOA, ATR/QM, and TRID regulations with PASS/CONDITIONAL_PASS/WARNING/FAIL status per regulation; the compliance check tool gates underwriting decisions -- FAIL status blocks approval; no other approach has structured compliance validation
- **Human-in-the-loop decisions:** Approach F implements a mandatory two-phase decision flow for underwriting decisions (`uw_render_decision` tool) -- Phase 1 (PROPOSE, `confirmed=false`) generates a proposal with decision type, stage transition, AI agreement, and warnings; Phase 2 (CONFIRM, `confirmed=true`) executes only after the underwriter explicitly approves; the agent's system prompt enforces never setting `confirmed=true` on the first call; no other approach has explicit human-in-the-loop decision gates
- **Vision-based document extraction:** Approach F includes a two-stage document extraction pipeline (`services/extraction.py`) -- Stage 1 uses pymupdf for text extraction from PDFs; if text is below `_MIN_TEXT_LENGTH = 50` chars (scanned/image-only PDF), Stage 2 falls back to LLM vision with base64-encoded images; extracted data is validated, document types are normalized, and HMDA demographic keywords trigger demographic routing; the vision model is configurable via `config/models.yaml` (separate tier from main LLM); Approach A has no document extraction
- **MCP server:** Approach A uses MCP servers as general-purpose tool providers; Approach F deploys a FastMCP risk assessment server (`mcp_server.py`) with Streamable HTTP transport exposing 6 pure-computation tools (DTI calculation, LTV ratio, credit risk evaluation, income stability assessment, asset sufficiency) for the underwriter agent via `langchain-mcp-adapters` (`agents/mcp_integration.py`); an optional second MCP server for predictive ML model integration is supported
- **A2A protocol:** Approach F optionally exposes each agent as an A2A-compatible endpoint via Kagenti (`a2a_server.py`) using the `a2a` SDK -- feature-gated by `KAGENTI_ENABLED` env var and deployed via `AgentRuntime` CR (`kagenti-agentruntime.yaml`); includes SPIRE SVID identity for mTLS authentication; Approach A has no A2A/agent-to-agent protocol support
- **Model routing:** Approach A uses framework-managed model selection; Approach F defines tiered model routing in `config/models.yaml` with three tiers -- `llm` (primary chat), `vision` (document extraction, falls back to llm tier when unset), and `embedding` (compliance KB, defaults to in-process sentence-transformers with `nomic-ai/nomic-embed-text-v1.5`); config supports `${ENV_VAR:-default}` substitution with nested reference resolution and hot-reload via mtime check
- **Model serving:** Approach A deploys vLLM/LlamaStack for self-hosted inference; Approach F is model-server-agnostic -- connects to any OpenAI-compatible endpoint (vLLM, LlamaStack, MaaS, OpenAI) via `LLM_BASE_URL`; optional LlamaStack sidecar deployment for tool calling (`llamastack.enabled` in Helm values), optional KServe InferenceService endpoint (`llamastack.inferenceService.enabled`)
- **Authentication:** Approach A has no authentication; Approach F includes full Keycloak integration -- realm `mortgage-ai`, client `mortgage-ai-ui`, JWT validation on all endpoints, WebSocket authentication via `?token=<jwt>` query param (`_chat_handler.py`), dev mode bypass via `AUTH_DISABLED=true`
- **Frontend:** Approach A uses React/PatternFly; Approach F uses React with Radix UI component library, TanStack Router for routing, and Tailwind CSS for styling (`packages/ui/`)
- **Observability:** Approach A has no observability; Approach F includes MLflow autolog for LangGraph/LangChain tracing with three authentication modes (Kubernetes plugin for RHOAI 3.4+, explicit token, mounted ServiceAccount), custom Prometheus metrics (`core/metrics.py`) tracking LLM token usage (per-model, per-direction, per-persona), inference latency histograms, agent routing decisions, tool call duration and counts, and active WebSocket sessions; prometheus-fastapi-instrumentator provides automatic HTTP metrics
- **Evaluation:** Approach F includes three evaluation tiers: (1) MLflow GenAI scorers (`evaluations/run_agent_eval.py`) with both simple deterministic checks (custom `contains_expected`, `has_numeric_result` scorers) and LLM-as-a-judge mode (RelevanceToQuery, Safety, ToolCallCorrectness, Guidelines); (2) KFP pipeline (`evaluations/kfp_eval_pipeline.py`) with modular steps (setup_mlflow, create_dataset, run_simple_eval, run_llm_judge_eval) for OpenShift-native evaluation; (3) TrustyAI EvalHub CR (`evaluations/evalhub/`) deploying EvalHub + DSPA + MLflow with SQLite backend and 3 providers, patching RBAC gaps in the TrustyAI operator; no other approach has three-tier evaluation
- **Helm chart:** Approach A uses custom Helm; Approach F uses a standalone chart (`deploy/helm/mortgage-ai/`) with no ai-architecture-charts subcharts -- all components (API, UI, database, MinIO, Keycloak, MLflow RBAC, NeMo Guardrails with content safety model, MCP risk server, MCP weather server, LlamaStack, Kagenti AgentRuntime, seed job) are defined in a single chart with feature toggles (`nemoGuardrails.enabled`, `kagenti.enabled`, `llamastack.enabled`, `keycloak.enabled`, `minio.enabled`, `database.enabled`, `mlflow.rbac.enabled`)
- **Local development:** Approach F includes `compose.yml` for local development with PostgreSQL 16 + pgvector, supporting both Podman and Docker (requires `uv` for Python, `pnpm` for Node.js)

### Typical Components

- **Model serving:** Any OpenAI-compatible LLM endpoint (vLLM, LlamaStack, MaaS, OpenAI); optional vision model on separate tier for document extraction; in-process sentence-transformers (`nomic-ai/nomic-embed-text-v1.5`) for compliance KB embeddings or remote OpenAI-compatible embedding endpoint; optional NeMo Guardrails with dedicated NemoGuard 8B content safety model via vLLM
- **Backend:** FastAPI (`packages/api/`) with 5 LangGraph agents (public, borrower, loan officer, underwriter, CEO), each with YAML-defined config (`config/agents/`), RBAC-scoped tools, and WebSocket streaming; NeMo Guardrails input/output shield nodes in the LangGraph graph; compliance services (HMDA, checks, KB search); audit trail with hash chaining; PII masking middleware; MCP client for risk assessment tools
- **Frontend:** React (`packages/ui/`) with Radix UI components, TanStack Router, and Tailwind CSS; persona-specific views for each user role
- **Data layer:** PostgreSQL 16 + pgvector for application data (applications, borrowers, financials, documents, conditions, decisions), audit events (hash-chained), HMDA demographics (isolated schema), compliance KB embeddings (tier-boosted vector search), and conversation history
- **Supporting:** Keycloak for SSO with realm, client, and role-based access; MinIO for document storage (S3-compatible); MCP risk assessment server (FastMCP, Streamable HTTP) with DTI, LTV, credit risk, income stability, and asset sufficiency tools; optional predictive ML model MCP server; optional Kagenti AgentRuntime CR for A2A protocol; MLflow for LangGraph/LangChain tracing with Kubernetes auth; Prometheus custom metrics (LLM tokens, inference latency, agent routing, tool calls); seed job for demo data; three-tier evaluation (MLflow GenAI + KFP + EvalHub)

---

## Approach G: MCP-Server-Centric Observability Agent with Console Plugin (from openshift-ai-observability-summarizer)

### When to Use

When the agentic app's primary purpose is turning platform observability signals (Prometheus metrics, distributed traces, logs, signal correlations) into actionable natural-language insights, the application itself IS the MCP tool server (not a consumer of external MCP servers), the agent layer is a multi-provider chatbot factory supporting external LLM providers (OpenAI, Anthropic, Google, Meta) without requiring self-hosted model inference for the chat function, and the UI integrates natively into the OpenShift Console as a dynamic plugin.

### Differences from Approach A

- **MCP role inversion:** Approach A uses MCP servers as external tool providers consumed by the agent; Approach G inverts this -- the application IS a FastMCP server (`src/mcp_server/observability_mcp.py`) that registers 40+ tools and the chatbot layer calls those tools internally via `MCPServerAdapter` (`src/mcp_server/mcp_tools_adapter.py`), which wraps the `ObservabilityMCPServer` instance to provide the `ToolExecutor` interface for in-process tool execution
- **Agent framework:** Approach A uses pluggable multi-framework runners (LlamaStack, LangGraph, CrewAI); Approach G uses no agent framework -- multi-provider chatbot implementations (`src/chatbots/openai_bot.py`, `anthropic_bot.py`, `google_bot.py`, `llama_bot.py`, `deterministic_bot.py`) each implement provider-native tool calling directly using their respective SDKs (OpenAI SDK, Anthropic SDK, Google GenAI SDK, LlamaStack client), with a factory function (`src/chatbots/factory.py`) selecting the appropriate chatbot based on model name prefix
- **Multi-provider LLM support:** Approach A deploys self-hosted models (vLLM/LlamaStack) for inference; Approach G supports both self-hosted models (via optional RAG stack with llm-service + llama-stack subcharts) and external providers (OpenAI, Anthropic, Google) via API keys, with provider-specific chatbot implementations handling tool calling format differences; per-model API keys can be stored in Kubernetes Secrets or browser-cached in DEV_MODE (`src/core/api_key_manager.py`)
- **Tool domain:** Approach A provides general-purpose tools; Approach G provides domain-specific observability tools organized in 5 tool modules:
  - `prometheus_tools.py` -- search metrics, get metadata, execute PromQL, explain results, suggest queries, select best metric, search by category, convert time durations
  - `observability_openshift_tools.py` -- analyze OpenShift clusters, fetch metrics data, list namespaces, list metric groups, chat with OpenShift context
  - `observability_vllm_tools.py` -- list models/namespaces, get model config, fetch vLLM metrics, analyze vLLM, calculate metrics, list summarization models, get GPU/deployment info, chat with vLLM context
  - `tempo_tools.py` -- query Tempo traces, get trace details, chat with Tempo context
  - `korrel8r_tools.py` -- signal correlation across metrics, traces, and logs via Korrel8r REST API
- **Metrics catalog:** Approach G includes a curated and dynamically validated metrics catalog (`src/core/metrics_catalog.py`) that eagerly initializes on server startup, validates metrics against the live Prometheus instance, and organizes metrics into categories for browsable UI navigation and accurate tool selection
- **Question classification:** Approach G implements centralized question classification (`src/core/question_classification.py`) that extracts Kubernetes namespaces from natural language questions via regex patterns, classifies question types (error traces, slow traces, performance analysis, alert analysis), and routes to appropriate tool chains
- **Frontend:** Approach A uses React/PatternFly as a standalone web app; Approach G provides two deployment modes -- an OpenShift Console dynamic plugin (`openshift-plugin/plugin/`) that adds an "AI Observability" menu item to the console left navigation (production default, `DEV_MODE=false`), or a standalone React UI (`openshift-plugin/react-ui/`) accessible via a Route (development, `DEV_MODE=true`); both modes share the same core React components (`openshift-plugin/shared/`)
- **Lifecycle management:** Approach A uses custom Helm charts; Approach G uses a Helm Operator (`deploy/operator/`, operator-framework helm-operator v1.37.0) that manages an `AIObservabilitySummarizer` CR (`apiVersion: aiobs.rh-ai-quickstart.io/v1alpha1`, `kind: AIObservabilitySummarizer` per `deploy/operator/watches.yaml`) with the umbrella `aiobs-stack` chart as the watched Helm chart, enabling OLM-based lifecycle management; the operator includes OLM bundle and catalog artifacts (`deploy/operator/bundle/`, `deploy/operator/catalog/`)
- **Umbrella chart:** Approach G deploys via a single `aiobs-stack` umbrella chart (`deploy/helm/aiobs-stack/Chart.yaml`) with 10 subchart dependencies organized into infrastructure (MinIO, TempoStack, LokiStack, OTel Collector, Korrel8r -- always installed to fixed namespaces) and application (MCP Server, Console Plugin, Alerting, RAG -- conditionally enabled via `mcpServer.enabled`, `consolePlugin.enabled`, `alerting.enabled`, `rag.enabled`)
- **Alerting:** Approach A has no alerting; Approach G includes a CronJob-based alerting component (`src/alerting/alert_receiver.py`) that polls Alertmanager API (`/api/v2/alerts`) for active alerts, summarizes them using a LlamaStack client, and sends formatted summaries to a Slack webhook; deployed via a separate `alerts` Helm subchart (`deploy/helm/alerting/`)
- **Report generation:** Approach G generates shareable reports in HTML, PDF, and Markdown formats (`src/core/report_assets/report_renderer.py`) with metric cards, charts, and analysis summaries; reports are saved with UUID-based IDs to `/tmp/reports/` and served via FastAPI file endpoints; no other approach has built-in report generation
- **Observability stack (consumed, not emitted):** Unlike Approach E which instruments the agent for observability, Approach G consumes the cluster's observability stack as its data source -- it queries Prometheus/Thanos for metrics, Tempo for traces, Loki for logs (via Korrel8r correlation), and DCGM exporter for GPU telemetry; the observability components (TempoStack, LokiStack, OTel Collector, MinIO, Korrel8r) are deployed by the umbrella chart as infrastructure dependencies
- **Signal correlation:** Approach G integrates Korrel8r (`src/core/korrel8r_client.py`) for cross-domain signal correlation -- given a metric alert or anomaly, Korrel8r finds related traces, logs, and other signals across the observability stack; the client forwards bearer tokens for Korrel8r to impersonate to stores (Prometheus, Tempo, Loki)
- **MCP transport:** Approach G supports both SSE and stateless HTTP MCP transport protocols (`settings.MCP_TRANSPORT_PROTOCOL`), with SSL support for in-cluster TLS termination (`MCP_SSL_KEYFILE`, `MCP_SSL_CERTFILE`); the Route uses `reencrypt` TLS termination (`deploy/helm/mcp-server/values.yaml`)
- **Model serving:** Approach A deploys vLLM/LlamaStack for self-hosted inference within the application; Approach G deploys the RAG stack (llm-service + llama-stack + pgvector) as an optional subchart (`rag.enabled` in aiobs-stack) via ai-architecture-charts, with llama-stack supporting both Helm chart deployment (`managedByOperator: false`) and operator-managed LlamaStackDistribution CR (`managedByOperator: true` for RHOAI 3.x); the MCP server itself connects to any LLM via the chatbot factory, including external providers via `LLM_URL` environment variable
- **RBAC:** Approach G requires cluster-admin for deployment due to console plugin installation, cluster-wide observability RBAC (ClusterRoleBinding in `deploy/helm/mcp-server/templates/crb.yaml` for Prometheus/Thanos/Tempo/Loki access), and cross-namespace infrastructure deployment

### Typical Components

- **Model serving:** Optional RAG stack via ai-architecture-charts subcharts (llm-service + llama-stack + pgvector) for self-hosted LLM inference; alternatively connects to external providers (OpenAI, Anthropic, Google, MaaS) via API keys configured in UI settings or Kubernetes Secrets
- **Backend:** FastMCP server (`src/mcp_server/`) providing 40+ MCP tools for Prometheus, OpenShift, vLLM, Tempo, and Korrel8r analysis; FastAPI wrapping MCP app with report endpoints; multi-provider chatbot factory (`src/chatbots/`) with provider-specific tool calling implementations; MCPServerAdapter for in-process tool execution; alerting CronJob (`src/alerting/`) polling Alertmanager and summarizing via LlamaStack
- **Frontend:** OpenShift Console dynamic plugin (`openshift-plugin/plugin/`) adding native "AI Observability" menu item with dashboards, chat, and settings; standalone React UI (`openshift-plugin/react-ui/`) for development mode; shared component library (`openshift-plugin/shared/`)
- **Data layer:** Prometheus/Thanos for time-series metrics (queried via PromQL), TempoStack for distributed traces, LokiStack for log aggregation, Korrel8r for cross-signal correlation; optional pgvector for RAG document storage
- **Supporting:** Helm Operator (AIObservabilitySummarizer CR) for OLM-based lifecycle management with umbrella aiobs-stack chart; MinIO for observability storage (Tempo/Loki backends); OTel Collector for trace/metric collection; curated metrics catalog with dynamic validation; report generation (HTML/PDF/Markdown); Makefile with `install`, `enable-tracing-ui`, `enable-logging-ui` targets

---

## Choosing Between Approaches

| Criteria | Approach A (Conversational Agent Platform) | Approach B (Event-Driven Agent Pipeline) | Approach C (MCP-Native Agent with Provider Abstraction) | Approach D (CloudEvent-Driven Multi-Microservice Platform) | Approach E (Operator-Managed Agent with Observability) | Approach F (Multi-Persona Agent with Compliance Controls) | Approach G (MCP-Server-Centric Observability Agent) |
|----------|-------------------------------------------|------------------------------------------|--------------------------------------------------------|-----------------------------------------------------------|-------------------------------------------------------|----------------------------------------------------------|------------------------------------------------------|
| Triggering | User-initiated conversation | External events (Grafana alerts, webhooks) | User-initiated conversation | Multi-channel (Slack, email, Zammad, CLI, webhooks) | User-initiated via Playground UI or Jupyter notebooks | User-initiated conversation via role-specific WebSocket endpoints | User-initiated via Console Plugin or React UI; event-driven via alerting CronJob (Alertmanager polling) |
| Agent framework | Multi-framework (LlamaStack, LangGraph, CrewAI) | LangGraph only | None (MCP-Direct) or Llama Stack (delegated) | LangGraph with YAML-configurable state machines | LlamaStack (operator-managed LlamaStackDistribution CR) | LangGraph with YAML-defined agent configs per persona | None -- multi-provider chatbot factory with provider-native tool calling (OpenAI SDK, Anthropic SDK, Google GenAI SDK, LlamaStack client) |
| Agent topology | Single agent with tool access | Main graph with nested sub-agents | Single agent with tool allowlist | Router agent + specialist agents (routing-agent hands off to domain agents) | Single agent with MCP tool access via LlamaStack | 5 persona-specific agents (public, borrower, loan officer, underwriter, CEO) selected by authenticated user role | Single chatbot instance per request with MCPServerAdapter providing in-process tool execution |
| Pipeline | Open-ended conversational tool use | Fixed domain-specific multi-step flow | Domain-specific conversational tool use (database governance) | Configurable per-agent state machine flows defined in YAML | Open-ended conversational tool use with full trace visibility | Domain-specific conversational tool use with per-persona workflows (intake, document review, risk assessment, compliance, decisions) | Domain-specific observability analysis -- question classification, namespace extraction, metric selection via catalog, PromQL execution, LLM summarization, report generation |
| Service architecture | Monolithic FastAPI | Monolithic FastAPI + services | Monolithic FastAPI | Three microservices (agent-service, request-manager, integration-dispatcher) | No custom backend -- operator-managed LlamaStackDistribution CR + standalone MCP Helm charts | Monolithic FastAPI monorepo (packages/api/ + packages/ui/) with MCP sidecar server | FastMCP server wrapped in FastAPI with OpenShift Console Plugin frontend; Helm Operator managing umbrella chart; separate alerting CronJob |
| Inter-service communication | Direct HTTP | Direct HTTP | Direct HTTP | CloudEvents via Knative Eventing + Kafka Broker | Direct HTTP (LlamaStack to MCP servers via SSE) | Direct HTTP + MCP Streamable HTTP for risk tools; optional A2A protocol via Kagenti | In-process (MCPServerAdapter calls tools directly); HTTP to Prometheus/Thanos, Tempo, Korrel8r, Alertmanager |
| Model serving | Self-hosted vLLM/LlamaStack | API-only (OpenAI-compatible endpoint) | KServe + vLLM with optional Llama Stack | vLLM + LlamaStack via ai-architecture-charts subcharts | KServe + custom OTel-instrumented vLLM + operator-managed LlamaStack (GPU and Xeon CPU device types) | Any OpenAI-compatible endpoint (model-server-agnostic) with tiered routing (llm, vision, embedding via config/models.yaml) | Multi-provider (OpenAI, Anthropic, Google, Meta via API keys) or self-hosted via optional RAG subchart (llm-service + llama-stack via ai-architecture-charts); LLM_URL for existing endpoints |
| Frontend | React/PatternFly | Gradio | SvelteKit + Vega-Lite | CLI + Slack + email + Zammad (no custom web UI) | Llama Stack Playground (Streamlit) + Jupyter notebooks | React (Radix UI + TanStack Router + Tailwind CSS) with persona-specific views | OpenShift Console dynamic plugin (native console menu item, DEV_MODE=false) or standalone React UI (Route-accessible, DEV_MODE=true) |
| RAG | pgvector knowledge bases | FAISS cheat sheet from domain docs | None (database accessed via MCP tools) | LlamaStack vector stores with YAML-configured knowledge bases | Inline Milvus (SQLite-backed) with inline sentence-transformers | pgvector compliance KB with tier-boosted search (federal 1.5x > agency 1.2x > internal 1.0x) | Optional (pgvector + llama-stack via rag subchart); primary data source is live observability signals, not documents |
| Guardrails | Configurable within agent framework | Not included | Not included | NVIDIA NeMo Guardrails + NemoGuard JailbreakDetect NIM (optional) | Llama Guard 3-1B via dedicated KServe InferenceService | NeMo Guardrails input/output shield nodes in LangGraph graph using /v1/guardrail/checks API; fail-closed on error; NemoGuard 8B content safety model (optional) | Not included |
| Observability | Not included | Arize Phoenix + Grafana + Alloy | Not included | Langfuse + MLflow + OpenTelemetry + ClickHouse | Full stack: OTel sidecars (vLLM + LlamaStack), TempoStack, Grafana (vLLM dashboard), UWM PodMonitors, OCP Distributed Tracing UI Plugin | MLflow autolog for LangGraph/LangChain (Kubernetes auth for RHOAI 3.4+) + Prometheus custom metrics (LLM tokens, inference latency, agent routing, tool calls, sessions) | Consumed as data source (Prometheus/Thanos, TempoStack, LokiStack, Korrel8r, DCGM) -- deploys infrastructure components via umbrella chart; not instrumented for self-observability |
| Evaluation | Not included | Not included | Not included | DeepEval-based evaluation pipeline with synthetic and real conversations | Jupyter notebook for evals + LlamaStack inline eval/scoring providers | Three-tier: MLflow GenAI scorers (simple + LLM-as-a-judge) + KFP pipeline + TrustyAI EvalHub CR with DSPA | Not included |
| Tool security | Framework-managed | Framework-managed | Hard-coded allowlist + Pydantic validation | Framework-managed via LlamaStack + MCP require_approval config | Framework-managed via LlamaStack; OpenShift MCP server uses read-only RBAC ClusterRole | Per-tool allowed_roles in YAML agent config + per-role DataScope filtering + PII masking middleware + HMDA data isolation | ClusterRoleBinding for read-only access to Prometheus/Thanos, Tempo, Loki; no per-user tool restrictions |
| Authentication | Not included | Not included | Not included | Not included | Not included | Keycloak SSO with JWT validation, WebSocket token auth, role-based endpoint access, dev mode bypass | Not included (inherits OpenShift Console authentication for plugin mode) |
| Audit trail | Not included | Not included | Not included | Not included | Not included | SHA-256 hash-chained append-only audit events with PostgreSQL advisory lock serialization and LangFuse session correlation | Not included |
| Human-in-the-loop | Not included | Not included | Not included | Not included | Not included | Two-phase decision flow (PROPOSE then CONFIRM) for underwriting decisions; compliance check gate blocks approval on FAIL | Not included |
| Best for | General-purpose agent with interactive tool use | Automated domain-specific analysis pipelines | Domain-specific agent with direct MCP integration and switchable orchestration modes | Production enterprise IT self-service with multi-channel input, extensible YAML-driven agent workflows, and event-driven microservice architecture | Observability-first agent deployment requiring end-to-end tracing, metrics, and dashboards for LLM audit, compliance, and performance monitoring with zero custom backend code | Regulated-industry multi-persona agent platform requiring per-role RBAC, compliance controls, PII masking, audit trails, human-in-the-loop decisions, and vision-based document extraction | Platform observability intelligence -- turning Prometheus metrics, traces, logs, and GPU telemetry into natural-language insights with native OpenShift Console integration and Helm Operator lifecycle management |
